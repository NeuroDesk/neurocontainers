name: auto-build

on:
  push:
    paths:
      - recipes/**
    branches:
      - master

jobs:
  applist:
    runs-on: ubuntu-22.04
    outputs:
      apps_to_build: ${{ steps.find_changed_recipes.outputs.apps_to_build }}
    steps:
    - uses: actions/checkout@v3
      with:
        fetch-depth: 0
    - name: Find changed directories
      id: find_changed_dirs
      uses: tj-actions/changed-files@v34
      with:
        files: "recipes/**"
        dir_names: "true"
        json: "true"
    - name: Find changed recipes
      id: find_changed_recipes
      if: steps.find_changed_dirs.outputs.any_changed == 'true'
      run: |
        # Get dir changelist -> convert list to json -> extract app name only -> remove duplicate apps
        changed_recipes=$(echo "${{ steps.find_changed_dirs.outputs.all_changed_files }}" | jq -rc '.[]'  | cut -d/ -f 2-2 | jq -ncR '[inputs]' | jq -rc 'unique')
        
        apps_to_build='['
        for APPLICATION in $(echo "${changed_recipes}" | jq -r '.[]'); do
          AUTOBUILD=$(cat .github/workflows/build-config.json | jq ".${APPLICATION} .autoBuild")
          if [ "$AUTOBUILD" = "true" ] || [ "$AUTOBUILD" = "null" ]; then
              apps_to_build+="\"${APPLICATION}\","
          fi
        done
        apps_to_build=$(sed '$s/,$/]/' <<< $apps_to_build)
        echo "apps_to_build=${apps_to_build}" | tee $GITHUB_OUTPUT

  builder:
    needs: applist
    uses: ./.github/workflows/builder.yml
    with:
      applications: ${{ needs.applist.outputs.apps_to_build }}
